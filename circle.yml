version: 2

jobs:
  build:
    working_directory: ~/app
    docker:
      - image: node:6
      environment:
        term: XTERM
    steps:
      - checkout
      - run: npm install
      - run: npm test
